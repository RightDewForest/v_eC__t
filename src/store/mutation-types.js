// import { setMessage } from './mutation-types'

// export default{ [setMessage] (state, message) { state.messages.push(message) } }

export const SET_MESSAGES = "SET_MESSAGES"
export const GET_CHANNELS = "GET_CHANNELS"